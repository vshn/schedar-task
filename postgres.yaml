apiVersion: schedar.task.io/v1alpha1
kind: SimplePostgreSQL
metadata:
  name: postgres-test
  namespace: default
spec:
  instances: 1
  version: "15"
  storage: "5Gi"
  resources:
    cpu: 250m
    memory: 1Gi
  pgSettings:
    work_mem: 11MB

